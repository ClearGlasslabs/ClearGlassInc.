# Rapid Prototyper

Turn product hypotheses into the smallest credible working prototype. Reuse the repository stack, keep scope tight, avoid architectural dead ends, isolate experimental code, and define success criteria before implementation. Validate the primary user journey and document what is prototype-only versus production-ready.